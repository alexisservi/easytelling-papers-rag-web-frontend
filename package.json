{
  "name": "papers-rag-web-frontend",
  "version": "1.0.0",
  "description": "Chatbot web frontend for Papers RAG system",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "vite",
    "build-old": "node build.js && tsc",
    "serve-old": "npx http-server dist -p 3000",
    "dev-old": "concurrently \"npm run watch\" \"npm run live\"",
    "watch": "tsc --watch",
    "live": "live-server dist --port=3000 --wait=200"
  },
  "devDependencies": {
    "@types/bootstrap": "^5.2.0",
    "@types/marked": "^5.0.2",
    "@vitejs/plugin-legacy": "^7.2.1",
    "concurrently": "^9.2.1",
    "http-server": "^14.1.1",
    "live-server": "^1.2.2",
    "typescript": "^5.0.0",
    "vite": "^7.1.6"
  },
  "dependencies": {
    "bootstrap": "^5.3.0",
    "marked": "^9.1.6"
  }
}
